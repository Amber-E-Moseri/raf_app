export const APP_NAME = "RAF";
export const DEFAULT_PAGE_SIZE = 10;
export const SUPPORTED_CATEGORY_ENDPOINTS = [
  "/allocation-categories",
  "/household/allocation-categories",
] as const;
export const SUPPORTED_CATEGORY_UPDATE_ENDPOINTS = [
  "/allocation-categories",
  "/household/allocation-categories",
] as const;
